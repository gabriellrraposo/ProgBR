// let calc = {
//     soma: (x, y) => { return x + y },
//     subtrai: (x, y) => { return x - y },
//     multiplica: (x, y) => { return x * y },
//     divide: (x, y) => { return x / y }
// }

// module.exports = calc 

// OU

module.exports.soma = (x, y) => { return x + y }
module.exports.multiplica = (x, y) => { return x * y }
module.exports.divide = (x, y) => { return x / y }
module.exports.subtrai = (x, y) => { return x - y }
//Método para Common JS, precisa criar um objeto e instanciar na outra página